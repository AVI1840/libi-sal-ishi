# Contributing to סל אישי

תודה על העניין בתרומה לפרויקט סל אישי.

## מצב הפרויקט

סל אישי הוא כרגע **דמו אסטרטגי מבוקר** — בסיס לפיילוט אמיתי. הלוגיקה העסקית (המלצות, סבסוד, CRM) עובדת. שכבת הנתונים היא mock.

ראו `docs/truth-map.md` למפת מצב מלאה.

## עקרונות מנחים

- לא לשבור לוגיקה עסקית קיימת
- לשמור על עברית כשפת ממשק ראשית (RTL)
- לא להוסיף נתונים אישיים אמיתיים
- לשמור על פשטות
- להבדיל תמיד בין Demo, Pilot, ו-Production
- לסמן mock data במפורש

## מבנה הפרויקט

```
docs/                    # מסמכי מדיניות, ארכיטקטורה, פיילוט
frontend/
  case-manager-dashboard/  # ממשק מתאמת (React + TypeScript + Tailwind)
```

## איך לתרום

### דיווח על בעיות
פתח Issue עם תיאור, שלבים לשחזור, וסביבה.

### הצעת שיפורים
פתח Issue עם תג `enhancement` לפני שמתחילים לכתוב קוד.

### Pull Requests
1. Fork את הריפו
2. צור branch: `git checkout -b feature/your-feature`
3. ודא שהקוד עובד
4. פתח PR עם תיאור ברור

## קוד

- TypeScript: עקוב אחר הסגנון הקיים (ESLint, Prettier)
- אל תוסיף secrets או מידע אישי אמיתי
- שמור על RTL ועברית בכל הממשק

## שאלות

פתח Issue עם תג `question`.
